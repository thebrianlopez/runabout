# docs/.chain-index.json

The docs/.chain-index.json file was found. Its content:

```
{invalid json here - this file is malformed
  "schema_version": "1.0"
  missing closing brace and quote
```

This file contains malformed JSON that cannot be parsed.
